# CRM Sprint 4 Decision Record

Accepted decision: close Sprint 4 as foundation-only with `OverallDecision=GoForNonProductionFoundationPilot`.

Rejected decisions for now:

- Real DB runtime activation.
- EF runtime activation.
- Migrations.
- Portal Auth runtime activation.
- Productive CRM routes `/api/crm/leads`, `/api/crm/accounts`, `/api/crm/contacts`.
- DELETE endpoints.
- Productive CRM UI, login/logout, token storage or Identity.

Next decision: `Sprint5P1ControlledRuntimeProbeActivationPlan`.

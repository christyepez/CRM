# CRM Sensitive Data Policy

CRM Sprint 1 repository content must not include secrets, credentials, access tokens, refresh tokens, embed tokens, certificates, private keys, personal data, financial records, tax artifacts or environment-specific private URLs.

Configuration must remain external for any future productive activation.

Committed examples must stay conceptual or empty and must never represent real customers, invoices, tax identifiers, reports or BI workspaces.

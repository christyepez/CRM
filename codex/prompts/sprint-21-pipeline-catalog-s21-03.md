# CRM Sprint 21 S21-03 - Pipeline Catalog Foundation API

Expose read-only list/detail foundation endpoints under `/api/crm/foundation/pipelines` using `IPipelineCatalogService`.

Required: explicit response DTOs, list/detail GET only, 404 on missing, no POST/PUT/PATCH/DELETE, no productive `/api/crm/pipelines`, no Portal Catalog runtime, no Common DB/EF/SQL, no real data or production.
